{
  "name": "tsc-memory-match",
  "version": "1.0.0",
  "description": "tsc memory match but its all oop this time",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node_modules/.bin/webpack --config webpack.config.js --progress --colors"
  },
  "author": "stayasleep",
  "license": "ISC",
  "dependencies": {
    "awesome-typescript-loader": "^5.2.0",
    "source-map-loader": "^0.2.4",
    "typescript": "^3.0.1",
    "webpack": "^4.16.5"
  },
  "devDependencies": {
    "webpack-cli": "^3.1.0"
  }
}
